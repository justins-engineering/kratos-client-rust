/*
 * Ory Identities API
 *
 * This is the API specification for Ory Identities with features such as registration, login, recovery, account verification, profile settings, password reset, identity management, session management, email and sms delivery, and more.
 *
 * The version of the OpenAPI document: v25.4.0
 * Contact: office@ory.sh
 * Generated by: https://openapi-generator.tech
 */

use super::{AddQuery, ContentType, Error, configuration};
use crate::{models, wasm_apis::ResponseContent};
use gloo_utils::format::JsValueSerdeExt;
use serde::{Deserialize, Serialize, de::Error as _};
use wasm_bindgen::prelude::*;
use wasm_bindgen_futures::JsFuture;
use web_sys::{Request, RequestCredentials, RequestInit, RequestMode, Response};

/// struct for typed errors of method [`create_browser_login_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateBrowserLoginFlowError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_browser_logout_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateBrowserLogoutFlowError {
    Status400(models::ErrorGeneric),
    Status401(models::ErrorGeneric),
    Status500(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_browser_recovery_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateBrowserRecoveryFlowError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_browser_registration_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateBrowserRegistrationFlowError {
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_browser_settings_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateBrowserSettingsFlowError {
    Status400(models::ErrorGeneric),
    Status401(models::ErrorGeneric),
    Status403(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_browser_verification_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateBrowserVerificationFlowError {
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_fedcm_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateFedcmFlowError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_native_login_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateNativeLoginFlowError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_native_recovery_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateNativeRecoveryFlowError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_native_registration_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateNativeRegistrationFlowError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_native_settings_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateNativeSettingsFlowError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`create_native_verification_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum CreateNativeVerificationFlowError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`disable_my_other_sessions`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DisableMyOtherSessionsError {
    Status400(models::ErrorGeneric),
    Status401(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`disable_my_session`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum DisableMySessionError {
    Status400(models::ErrorGeneric),
    Status401(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`exchange_session_token`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ExchangeSessionTokenError {
    Status403(models::ErrorGeneric),
    Status404(models::ErrorGeneric),
    Status410(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_flow_error`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetFlowErrorError {
    Status403(models::ErrorGeneric),
    Status404(models::ErrorGeneric),
    Status500(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_login_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetLoginFlowError {
    Status403(models::ErrorGeneric),
    Status404(models::ErrorGeneric),
    Status410(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_recovery_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetRecoveryFlowError {
    Status404(models::ErrorGeneric),
    Status410(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_registration_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetRegistrationFlowError {
    Status403(models::ErrorGeneric),
    Status404(models::ErrorGeneric),
    Status410(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_settings_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetSettingsFlowError {
    Status401(models::ErrorGeneric),
    Status403(models::ErrorGeneric),
    Status404(models::ErrorGeneric),
    Status410(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_verification_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetVerificationFlowError {
    Status403(models::ErrorGeneric),
    Status404(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`get_web_authn_java_script`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum GetWebAuthnJavaScriptError {
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`list_my_sessions`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ListMySessionsError {
    Status400(models::ErrorGeneric),
    Status401(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`perform_native_logout`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum PerformNativeLogoutError {
    Status400(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`to_session`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum ToSessionError {
    Status401(models::ErrorGeneric),
    Status403(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`update_fedcm_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum UpdateFedcmFlowError {
    Status400(models::LoginFlow),
    Status410(models::ErrorGeneric),
    Status422(models::ErrorBrowserLocationChangeRequired),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`update_login_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum UpdateLoginFlowError {
    Status400(models::LoginFlow),
    Status410(models::ErrorGeneric),
    Status422(models::ErrorBrowserLocationChangeRequired),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`update_logout_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum UpdateLogoutFlowError {
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`update_recovery_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum UpdateRecoveryFlowError {
    Status400(models::RecoveryFlow),
    Status410(models::ErrorGeneric),
    Status422(models::ErrorBrowserLocationChangeRequired),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`update_registration_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum UpdateRegistrationFlowError {
    Status400(models::RegistrationFlow),
    Status410(models::ErrorGeneric),
    Status422(models::ErrorBrowserLocationChangeRequired),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`update_settings_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum UpdateSettingsFlowError {
    Status400(models::SettingsFlow),
    Status401(models::ErrorGeneric),
    Status403(models::ErrorGeneric),
    Status410(models::ErrorGeneric),
    Status422(models::ErrorBrowserLocationChangeRequired),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// struct for typed errors of method [`update_verification_flow`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum UpdateVerificationFlowError {
    Status400(models::VerificationFlow),
    Status410(models::ErrorGeneric),
    DefaultResponse(models::ErrorGeneric),
    UnknownValue(serde_json::Value),
}

/// This endpoint initializes a browser-based user login flow. This endpoint will set the appropriate cookies and anti-CSRF measures required for browser-based flows.  If this endpoint is opened as a link in the browser, it will be redirected to `selfservice.flows.login.ui_url` with the flow ID set as the query parameter `?flow=`. If a valid user session exists already, the browser will be redirected to `urls.default_redirect_url` unless the query parameter `?refresh=true` was set.  If this endpoint is called via an AJAX request, the response contains the flow without a redirect. In the case of an error, the `error.id` of the JSON response body can be one of:  `session_already_available`: The user is already signed in. `session_aal1_required`: Multi-factor auth (e.g. 2fa) was requested but the user has no session yet. `security_csrf_violation`: Unable to fetch the flow because a CSRF violation occurred. `security_identity_mismatch`: The requested `?return_to` address is not allowed to be used. Adjust this in the configuration!  The optional query parameter login_challenge is set when using Kratos with Hydra in an OAuth2 flow. See the oauth2_provider.url configuration option.  This endpoint is NOT INTENDED for clients that do not have a browser (Chrome, Firefox, ...) as cookies are needed.  More information can be found at [Ory Kratos User Login](https://www.ory.sh/docs/kratos/self-service/flows/user-login) and [User Registration Documentation](https://www.ory.sh/docs/kratos/self-service/flows/user-registration).
pub async fn create_browser_login_flow(
    configuration: &configuration::Configuration,
    refresh: Option<bool>,
    aal: Option<&str>,
    return_to: Option<&str>,
    cookie: Option<&str>,
    login_challenge: Option<&str>,
    organization: Option<&str>,
    via: Option<&str>,
    identity_schema: Option<&str>,
) -> Result<models::LoginFlow, Error<CreateBrowserLoginFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/login/browser".len()
        + (configuration.base_path.len() * 2)
        + "refresh".len()
        + "aal".len()
        + "return_to".len()
        + "login_challenge".len()
        + "organization".len()
        + "via".len()
        + "identity_schema".len()
        + 14;
    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/login/browser");

    let mut is_first_query: bool = true;

    if let Some(ref str) = refresh {
        uri_str.add_query(&mut is_first_query, "refresh=", &str.to_string());
    }
    if let Some(ref str) = aal {
        uri_str.add_query(&mut is_first_query, "aal=", &str);
    }
    if let Some(ref str) = return_to {
        uri_str.add_query(&mut is_first_query, "return_to=", &str);
    }
    if let Some(ref str) = login_challenge {
        uri_str.add_query(&mut is_first_query, "login_challenge=", &str);
    }
    if let Some(ref str) = organization {
        uri_str.add_query(&mut is_first_query, "organization=", &str);
    }
    if let Some(ref str) = via {
        uri_str.add_query(&mut is_first_query, "via=", &str);
    }
    if let Some(ref str) = identity_schema {
        uri_str.add_query(&mut is_first_query, "identity_schema=", &str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::LoginFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::LoginFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<CreateBrowserLoginFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint initializes a browser-based user logout flow and a URL which can be used to log out the user.  This endpoint is NOT INTENDED for API clients and only works with browsers (Chrome, Firefox, ...). For API clients you can call the `/self-service/logout/api` URL directly with the Ory Session Token.  The URL is only valid for the currently signed in user. If no user is signed in, this endpoint returns a 401 error.  When calling this endpoint from a backend, please ensure to properly forward the HTTP cookies.
pub async fn create_browser_logout_flow(
    configuration: &configuration::Configuration,
    cookie: Option<&str>,
    return_to: Option<&str>,
) -> Result<models::LogoutFlow, Error<CreateBrowserLogoutFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/logout/browser".len()
        + (configuration.base_path.len() * 2)
        + "?return_to=".len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/logout/browser");

    if let Some(ref str) = return_to {
        uri_str.push_str("?return_to=");
        uri_str.push_str(&str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::LogoutFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::LogoutFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<CreateBrowserLogoutFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint initializes a browser-based account recovery flow. Once initialized, the browser will be redirected to `selfservice.flows.recovery.ui_url` with the flow ID set as the query parameter `?flow=`. If a valid user session exists, the browser is returned to the configured return URL.  If this endpoint is called via an AJAX request, the response contains the recovery flow without any redirects or a 400 bad request error if the user is already authenticated.  This endpoint is NOT INTENDED for clients that do not have a browser (Chrome, Firefox, ...) as cookies are needed.  More information can be found at [Ory Kratos Account Recovery Documentation](../self-service/flows/account-recovery).
pub async fn create_browser_recovery_flow(
    configuration: &configuration::Configuration,
    return_to: Option<&str>,
) -> Result<models::RecoveryFlow, Error<CreateBrowserRecoveryFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/recovery/browser".len()
        + (configuration.base_path.len() * 2)
        + "?return_to=".len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/recovery/browser");

    if let Some(ref str) = return_to {
        uri_str.push_str("?return_to=");
        uri_str.push_str(&str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::RecoveryFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::RecoveryFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<CreateBrowserRecoveryFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint initializes a browser-based user registration flow. This endpoint will set the appropriate cookies and anti-CSRF measures required for browser-based flows.  If this endpoint is opened as a link in the browser, it will be redirected to `selfservice.flows.registration.ui_url` with the flow ID set as the query parameter `?flow=`. If a valid user session exists already, the browser will be redirected to `urls.default_redirect_url`.  If this endpoint is called via an AJAX request, the response contains the flow without a redirect. In the case of an error, the `error.id` of the JSON response body can be one of:  `session_already_available`: The user is already signed in. `security_csrf_violation`: Unable to fetch the flow because a CSRF violation occurred. `security_identity_mismatch`: The requested `?return_to` address is not allowed to be used. Adjust this in the configuration!  If this endpoint is called via an AJAX request, the response contains the registration flow without a redirect.  This endpoint is NOT INTENDED for clients that do not have a browser (Chrome, Firefox, ...) as cookies are needed.  More information can be found at [Ory Kratos User Login](https://www.ory.sh/docs/kratos/self-service/flows/user-login) and [User Registration Documentation](https://www.ory.sh/docs/kratos/self-service/flows/user-registration).
pub async fn create_browser_registration_flow(
    configuration: &configuration::Configuration,
    return_to: Option<&str>,
    login_challenge: Option<&str>,
    after_verification_return_to: Option<&str>,
    organization: Option<&str>,
    identity_schema: Option<&str>,
) -> Result<models::RegistrationFlow, Error<CreateBrowserRegistrationFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/registration/browser".len()
        + (configuration.base_path.len() * 2)
        + "return_to".len()
        + "login_challenge".len()
        + "after_verification_return_to".len()
        + "organization".len()
        + "identity_schema".len()
        + 10;
    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/registration/browser");

    let mut is_first_query: bool = true;

    if let Some(ref str) = return_to {
        uri_str.add_query(&mut is_first_query, "return_to=", &str);
    }
    if let Some(ref str) = login_challenge {
        uri_str.add_query(&mut is_first_query, "login_challenge=", &str);
    }
    if let Some(ref str) = organization {
        uri_str.add_query(&mut is_first_query, "organization=", &str);
    }
    if let Some(ref str) = after_verification_return_to {
        uri_str.add_query(&mut is_first_query, "after_verification_return_to=", &str);
    }
    if let Some(ref str) = identity_schema {
        uri_str.add_query(&mut is_first_query, "identity_schema=", &str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::RegistrationFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::RegistrationFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<CreateBrowserRegistrationFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint initializes a browser-based user settings flow. Once initialized, the browser will be redirected to `selfservice.flows.settings.ui_url` with the flow ID set as the query parameter `?flow=`. If no valid Ory Kratos Session Cookie is included in the request, a login flow will be initialized.  If this endpoint is opened as a link in the browser, it will be redirected to `selfservice.flows.settings.ui_url` with the flow ID set as the query parameter `?flow=`. If no valid user session was set, the browser will be redirected to the login endpoint.  If this endpoint is called via an AJAX request, the response contains the settings flow without any redirects or a 401 forbidden error if no valid session was set.  Depending on your configuration this endpoint might return a 403 error if the session has a lower Authenticator Assurance Level (AAL) than is possible for the identity. This can happen if the identity has password + webauthn credentials (which would result in AAL2) but the session has only AAL1. If this error occurs, ask the user to sign in with the second factor (happens automatically for server-side browser flows) or change the configuration.  If this endpoint is called via an AJAX request, the response contains the flow without a redirect. In the case of an error, the `error.id` of the JSON response body can be one of:  `security_csrf_violation`: Unable to fetch the flow because a CSRF violation occurred. `session_inactive`: No Ory Session was found - sign in a user first. `security_identity_mismatch`: The requested `?return_to` address is not allowed to be used. Adjust this in the configuration!  This endpoint is NOT INTENDED for clients that do not have a browser (Chrome, Firefox, ...) as cookies are needed.  More information can be found at [Ory Kratos User Settings & Profile Management Documentation](../self-service/flows/user-settings).
pub async fn create_browser_settings_flow(
    configuration: &configuration::Configuration,
    return_to: Option<&str>,
    cookie: Option<&str>,
) -> Result<models::SettingsFlow, Error<CreateBrowserSettingsFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/settings/browser".len()
        + (configuration.base_path.len() * 2)
        + "?return_to=".len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/settings/browser");

    if let Some(ref str) = return_to {
        uri_str.push_str("?return_to=");
        uri_str.push_str(&str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::SettingsFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::SettingsFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<CreateBrowserSettingsFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint initializes a browser-based account verification flow. Once initialized, the browser will be redirected to `selfservice.flows.verification.ui_url` with the flow ID set as the query parameter `?flow=`.  If this endpoint is called via an AJAX request, the response contains the recovery flow without any redirects.  This endpoint is NOT INTENDED for API clients and only works with browsers (Chrome, Firefox, ...).  More information can be found at [Ory Kratos Email and Phone Verification Documentation](https://www.ory.sh/docs/kratos/self-service/flows/verify-email-account-activation).
pub async fn create_browser_verification_flow(
    configuration: &configuration::Configuration,
    return_to: Option<&str>,
) -> Result<models::VerificationFlow, Error<CreateBrowserVerificationFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/verification/browser".len()
        + (configuration.base_path.len() * 2)
        + "?return_to=".len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/verification/browser");

    if let Some(ref str) = return_to {
        uri_str.push_str("?return_to=");
        uri_str.push_str(&str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::VerificationFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::VerificationFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<CreateBrowserVerificationFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint returns a list of all available FedCM providers. It is only supported on the Ory Network.
pub async fn create_fedcm_flow(
    configuration: &configuration::Configuration,
) -> Result<models::CreateFedcmFlowResponse, Error<CreateFedcmFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/fed-cm/parameters".len() + configuration.base_path.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/fed-cm/parameters");

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::CreateFedcmFlowResponse`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::CreateFedcmFlowResponse`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<CreateFedcmFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Calling this endpoint invalidates all except the current session that belong to the logged-in user. Session data are not deleted.
pub async fn disable_my_other_sessions(
    configuration: &configuration::Configuration,
    x_session_token: Option<&str>,
    cookie: Option<&str>,
) -> Result<models::DeleteMySessionsCount, Error<DisableMyOtherSessionsError>> {
    let client = RequestInit::new();
    client.set_method("DELETE");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/sessions".len() + configuration.base_path.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/sessions");

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }
    if let Some(x_session_token) = x_session_token {
        req_builder
            .headers()
            .set("X-Session-Token", x_session_token)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::DeleteMySessionsCount`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::DeleteMySessionsCount`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<DisableMyOtherSessionsError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Calling this endpoint invalidates the specified session. The current session cannot be revoked. Session data are not deleted.
pub async fn disable_my_session(
    configuration: &configuration::Configuration,
    id: &str,
    x_session_token: Option<&str>,
    cookie: Option<&str>,
) -> Result<(), Error<DisableMySessionError>> {
    let client = RequestInit::new();
    client.set_method("DELETE");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let id = crate::wasm_apis::urlencode(id);
    let possible_uri_len = "/sessions/".len() + configuration.base_path.len() + id.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/sessions/");
    uri_str.push_str(&id);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }
    if let Some(x_session_token) = x_session_token {
        req_builder
            .headers()
            .set("X-Session-Token", x_session_token)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        Ok(())
    } else {
        let entity: Option<DisableMySessionError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

pub async fn exchange_session_token(
    configuration: &configuration::Configuration,
    init_code: &str,
    return_to_code: &str,
) -> Result<models::SuccessfulNativeLogin, Error<ExchangeSessionTokenError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/sessions/token-exchange".len()
        + configuration.base_path.len()
        + "?init_code=".len()
        + init_code.len()
        + "&return_to_code=".len()
        + return_to_code.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/sessions/token-exchange");

    uri_str.push_str("?init_code=");
    uri_str.push_str(&init_code);

    uri_str.push_str("&return_to_code=");
    uri_str.push_str(&return_to_code);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::SuccessfulNativeLogin`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::SuccessfulNativeLogin`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<ExchangeSessionTokenError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint returns the error associated with a user-facing self service errors.  This endpoint supports stub values to help you implement the error UI:  `?id=stub:500` - returns a stub 500 (Internal Server Error) error.  More information can be found at [Ory Kratos User User Facing Error Documentation](https://www.ory.sh/docs/kratos/self-service/flows/user-facing-errors).
pub async fn get_flow_error(
    configuration: &configuration::Configuration,
    id: &str,
) -> Result<models::FlowError, Error<GetFlowErrorError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len =
        "/self-service/errors".len() + configuration.base_path.len() + "?id=".len() + id.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/errors");

    uri_str.push_str("?id=");
    uri_str.push_str(&id);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::FlowError`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::FlowError`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<GetFlowErrorError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint returns a login flow's context with, for example, error details and other information.  Browser flows expect the anti-CSRF cookie to be included in the request's HTTP Cookie Header. For AJAX requests you must ensure that cookies are included in the request or requests will fail.  If you use the browser-flow for server-side apps, the services need to run on a common top-level-domain and you need to forward the incoming HTTP Cookie header to this endpoint:  ```js pseudo-code example router.get('/login', async function (req, res) { const flow = await client.getLoginFlow(req.header('cookie'), req.query['flow'])  res.render('login', flow) }) ```  This request may fail due to several reasons. The `error.id` can be one of:  `session_already_available`: The user is already signed in. `self_service_flow_expired`: The flow is expired and you should request a new one.  More information can be found at [Ory Kratos User Login](https://www.ory.sh/docs/kratos/self-service/flows/user-login) and [User Registration Documentation](https://www.ory.sh/docs/kratos/self-service/flows/user-registration).
pub async fn get_login_flow(
    configuration: &configuration::Configuration,
    id: &str,
    cookie: Option<&str>,
) -> Result<models::LoginFlow, Error<GetLoginFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len =
        "/self-service/login/flows".len() + configuration.base_path.len() + "?id=".len() + id.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/login/flows");

    uri_str.push_str("?id=");
    uri_str.push_str(&id);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::LoginFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::LoginFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<GetLoginFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint returns a recovery flow's context with, for example, error details and other information.  Browser flows expect the anti-CSRF cookie to be included in the request's HTTP Cookie Header. For AJAX requests you must ensure that cookies are included in the request or requests will fail.  If you use the browser-flow for server-side apps, the services need to run on a common top-level-domain and you need to forward the incoming HTTP Cookie header to this endpoint:  ```js pseudo-code example router.get('/recovery', async function (req, res) { const flow = await client.getRecoveryFlow(req.header('Cookie'), req.query['flow'])  res.render('recovery', flow) }) ```  More information can be found at [Ory Kratos Account Recovery Documentation](../self-service/flows/account-recovery).
pub async fn get_recovery_flow(
    configuration: &configuration::Configuration,
    id: &str,
    cookie: Option<&str>,
) -> Result<models::RecoveryFlow, Error<GetRecoveryFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/recovery/flows".len()
        + configuration.base_path.len()
        + "?id=".len()
        + id.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/recovery/flows");

    uri_str.push_str("?id=");
    uri_str.push_str(&id);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::RecoveryFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::RecoveryFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<GetRecoveryFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint returns a registration flow's context with, for example, error details and other information.  Browser flows expect the anti-CSRF cookie to be included in the request's HTTP Cookie Header. For AJAX requests you must ensure that cookies are included in the request or requests will fail.  If you use the browser-flow for server-side apps, the services need to run on a common top-level-domain and you need to forward the incoming HTTP Cookie header to this endpoint:  ```js pseudo-code example router.get('/registration', async function (req, res) { const flow = await client.getRegistrationFlow(req.header('cookie'), req.query['flow'])  res.render('registration', flow) }) ```  This request may fail due to several reasons. The `error.id` can be one of:  `session_already_available`: The user is already signed in. `self_service_flow_expired`: The flow is expired and you should request a new one.  More information can be found at [Ory Kratos User Login](https://www.ory.sh/docs/kratos/self-service/flows/user-login) and [User Registration Documentation](https://www.ory.sh/docs/kratos/self-service/flows/user-registration).
pub async fn get_registration_flow(
    configuration: &configuration::Configuration,
    id: &str,
    cookie: Option<&str>,
) -> Result<models::RegistrationFlow, Error<GetRegistrationFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/registration/flows".len()
        + configuration.base_path.len()
        + "?id=".len()
        + id.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/registration/flows");

    uri_str.push_str("?id=");
    uri_str.push_str(&id);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::RegistrationFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::RegistrationFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<GetRegistrationFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// When accessing this endpoint through Ory Kratos' Public API you must ensure that either the Ory Kratos Session Cookie or the Ory Kratos Session Token are set.  Depending on your configuration this endpoint might return a 403 error if the session has a lower Authenticator Assurance Level (AAL) than is possible for the identity. This can happen if the identity has password + webauthn credentials (which would result in AAL2) but the session has only AAL1. If this error occurs, ask the user to sign in with the second factor or change the configuration.  You can access this endpoint without credentials when using Ory Kratos' Admin API.  If this endpoint is called via an AJAX request, the response contains the flow without a redirect. In the case of an error, the `error.id` of the JSON response body can be one of:  `security_csrf_violation`: Unable to fetch the flow because a CSRF violation occurred. `session_inactive`: No Ory Session was found - sign in a user first. `security_identity_mismatch`: The flow was interrupted with `session_refresh_required` but apparently some other identity logged in instead.  More information can be found at [Ory Kratos User Settings & Profile Management Documentation](../self-service/flows/user-settings).
pub async fn get_settings_flow(
    configuration: &configuration::Configuration,
    id: &str,
    x_session_token: Option<&str>,
    cookie: Option<&str>,
) -> Result<models::SettingsFlow, Error<GetSettingsFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/settings/flows".len()
        + configuration.base_path.len()
        + "?id=".len()
        + id.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/settings/flows");

    uri_str.push_str("?id=");
    uri_str.push_str(&id);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }
    if let Some(x_session_token) = x_session_token {
        req_builder
            .headers()
            .set("X-Session-Token", x_session_token)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::SettingsFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::SettingsFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<GetSettingsFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint returns a verification flow's context with, for example, error details and other information.  Browser flows expect the anti-CSRF cookie to be included in the request's HTTP Cookie Header. For AJAX requests you must ensure that cookies are included in the request or requests will fail.  If you use the browser-flow for server-side apps, the services need to run on a common top-level-domain and you need to forward the incoming HTTP Cookie header to this endpoint:  ```js pseudo-code example router.get('/recovery', async function (req, res) { const flow = await client.getVerificationFlow(req.header('cookie'), req.query['flow'])  res.render('verification', flow) }) ```  More information can be found at [Ory Kratos Email and Phone Verification Documentation](https://www.ory.sh/docs/kratos/self-service/flows/verify-email-account-activation).
pub async fn get_verification_flow(
    configuration: &configuration::Configuration,
    id: &str,
    cookie: Option<&str>,
) -> Result<models::VerificationFlow, Error<GetVerificationFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/verification/flows".len()
        + configuration.base_path.len()
        + "?id=".len()
        + id.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/verification/flows");

    uri_str.push_str("?id=");
    uri_str.push_str(&id);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::VerificationFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::VerificationFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<GetVerificationFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint provides JavaScript which is needed in order to perform WebAuthn login and registration.  If you are building a JavaScript Browser App (e.g. in ReactJS or AngularJS) you will need to load this file:  ```html <script src=\"https://public-kratos.example.org/.well-known/ory/webauthn.js\" type=\"script\" async /> ```  More information can be found at [Ory Kratos User Login](https://www.ory.sh/docs/kratos/self-service/flows/user-login) and [User Registration Documentation](https://www.ory.sh/docs/kratos/self-service/flows/user-registration).
pub async fn get_web_authn_java_script(
    configuration: &configuration::Configuration,
) -> Result<String, Error<GetWebAuthnJavaScriptError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/.well-known/ory/webauthn.js".len() + configuration.base_path.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/.well-known/ory/webauthn.js");

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `String`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `String`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<GetWebAuthnJavaScriptError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoints returns all other active sessions that belong to the logged-in user. The current session can be retrieved by calling the `/sessions/whoami` endpoint.
pub async fn list_my_sessions(
    configuration: &configuration::Configuration,
    per_page: Option<i64>,
    page: Option<i64>,
    page_size: Option<i64>,
    page_token: Option<&str>,
    x_session_token: Option<&str>,
    cookie: Option<&str>,
) -> Result<Vec<models::Session>, Error<ListMySessionsError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/sessions".len()
        + configuration.base_path.len()
        + "per_page".len()
        + "page".len()
        + "page_size".len()
        + "page_token".len()
        + (i64::MAX.to_string().len() * 3)
        + 8;
    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/sessions");

    let mut is_first_query: bool = true;

    if let Some(ref str) = per_page {
        uri_str.add_query(&mut is_first_query, "per_page=", &str.to_string());
    }
    if let Some(ref str) = page {
        uri_str.add_query(&mut is_first_query, "page=", &str.to_string());
    }
    if let Some(ref str) = page_size {
        uri_str.add_query(&mut is_first_query, "page_size=", &str.to_string());
    }
    if let Some(ref str) = page_token {
        uri_str.add_query(&mut is_first_query, "page_token=", &str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }
    if let Some(x_session_token) = x_session_token {
        req_builder
            .headers()
            .set("X-Session-Token", x_session_token)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `Vec&lt;models::Session&gt;`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `Vec&lt;models::Session&gt;`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<ListMySessionsError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Uses the HTTP Headers in the GET request to determine (e.g. by using checking the cookies) who is authenticated. Returns a session object in the body or 401 if the credentials are invalid or no credentials were sent. When the request it successful it adds the user ID to the 'X-Kratos-Authenticated-Identity-Id' header in the response.  If you call this endpoint from a server-side application, you must forward the HTTP Cookie Header to this endpoint:  ```js pseudo-code example router.get('/protected-endpoint', async function (req, res) { const session = await client.toSession(undefined, req.header('cookie'))  console.log(session) }) ```  When calling this endpoint from a non-browser application (e.g. mobile app) you must include the session token:  ```js pseudo-code example ... const session = await client.toSession(\"the-session-token\")  console.log(session) ```  When using a token template, the token is included in the `tokenized` field of the session.  ```js pseudo-code example ... const session = await client.toSession(\"the-session-token\", { tokenize_as: \"example-jwt-template\" })  console.log(session.tokenized) // The JWT ```  Depending on your configuration this endpoint might return a 403 status code if the session has a lower Authenticator Assurance Level (AAL) than is possible for the identity. This can happen if the identity has password + webauthn credentials (which would result in AAL2) but the session has only AAL1. If this error occurs, ask the user to sign in with the second factor or change the configuration.  This endpoint is useful for:  AJAX calls. Remember to send credentials and set up CORS correctly! Reverse proxies and API Gateways Server-side calls - use the `X-Session-Token` header!  This endpoint authenticates users by checking:  if the `Cookie` HTTP header was set containing an Ory Kratos Session Cookie; if the `Authorization: bearer <ory-session-token>` HTTP header was set with a valid Ory Kratos Session Token; if the `X-Session-Token` HTTP header was set with a valid Ory Kratos Session Token.  If none of these headers are set or the cookie or token are invalid, the endpoint returns a HTTP 401 status code.  As explained above, this request may fail due to several reasons. The `error.id` can be one of:  `session_inactive`: No active session was found in the request (e.g. no Ory Session Cookie / Ory Session Token). `session_aal2_required`: An active session was found but it does not fulfil the Authenticator Assurance Level, implying that the session must (e.g.) authenticate the second factor.
pub async fn to_session(
    configuration: &configuration::Configuration,
    x_session_token: Option<&str>,
    cookie: Option<&str>,
    tokenize_as: Option<&str>,
) -> Result<models::Session, Error<ToSessionError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len =
        "/sessions/whoami".len() + (configuration.base_path.len() * 2) + "?tokenize_as=".len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/sessions/whoami");

    if let Some(ref str) = tokenize_as {
        uri_str.push_str("?tokenize_as=");
        uri_str.push_str(&str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }
    if let Some(x_session_token) = x_session_token {
        req_builder
            .headers()
            .set("X-Session-Token", x_session_token)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::Session`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::Session`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<ToSessionError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Use this endpoint to submit a token from a FedCM provider through `navigator.credentials.get` and log the user in. The parameters from `navigator.credentials.get` must have come from `GET self-service/fed-cm/parameters`.
pub async fn update_fedcm_flow(
    configuration: &configuration::Configuration,
    update_fedcm_flow_body: models::UpdateFedcmFlowBody,
) -> Result<models::SuccessfulNativeLogin, Error<UpdateFedcmFlowError>> {
    let client = RequestInit::new();
    client.set_method("POST");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/fed-cm/token".len() + configuration.base_path.len();
    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/fed-cm/token");

    client.set_body(&JsValue::from_serde(&update_fedcm_flow_body)?);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }

    req_builder.headers().set("Accept", "application/json")?;
    req_builder
        .headers()
        .set("Content-Type", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::SuccessfulNativeLogin`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::SuccessfulNativeLogin`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<UpdateFedcmFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Use this endpoint to complete a login flow. This endpoint behaves differently for API and browser flows.  API flows expect `application/json` to be sent in the body and responds with HTTP 200 and a application/json body with the session token on success; HTTP 410 if the original flow expired with the appropriate error messages set and optionally a `use_flow_id` parameter in the body; HTTP 400 on form validation errors.  Browser flows expect a Content-Type of `application/x-www-form-urlencoded` or `application/json` to be sent in the body and respond with a HTTP 303 redirect to the post/after login URL or the `return_to` value if it was set and if the login succeeded; a HTTP 303 redirect to the login UI URL with the flow ID containing the validation errors otherwise.  Browser flows with an accept header of `application/json` will not redirect but instead respond with HTTP 200 and a application/json body with the signed in identity and a `Set-Cookie` header on success; HTTP 303 redirect to a fresh login flow if the original flow expired with the appropriate error messages set; HTTP 400 on form validation errors.  If this endpoint is called with `Accept: application/json` in the header, the response contains the flow without a redirect. In the case of an error, the `error.id` of the JSON response body can be one of:  `session_already_available`: The user is already signed in. `security_csrf_violation`: Unable to fetch the flow because a CSRF violation occurred. `security_identity_mismatch`: The requested `?return_to` address is not allowed to be used. Adjust this in the configuration! `browser_location_change_required`: Usually sent when an AJAX request indicates that the browser needs to open a specific URL. Most likely used in Social Sign In flows.  More information can be found at [Ory Kratos User Login](https://www.ory.sh/docs/kratos/self-service/flows/user-login) and [User Registration Documentation](https://www.ory.sh/docs/kratos/self-service/flows/user-registration).
pub async fn update_login_flow(
    configuration: &configuration::Configuration,
    flow: &str,
    update_login_flow_body: models::UpdateLoginFlowBody,
    x_session_token: Option<&str>,
    cookie: Option<&str>,
) -> Result<models::SuccessfulNativeLogin, Error<UpdateLoginFlowError>> {
    let client = RequestInit::new();
    client.set_method("POST");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len =
        "/self-service/login".len() + configuration.base_path.len() + "?flow=".len() + flow.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/login");

    uri_str.push_str("?flow=");
    uri_str.push_str(&flow);

    client.set_body(&JsValue::from_serde(&update_login_flow_body)?);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }
    if let Some(x_session_token) = x_session_token {
        req_builder
            .headers()
            .set("X-Session-Token", x_session_token)?;
    }

    req_builder.headers().set("Accept", "application/json")?;
    req_builder
        .headers()
        .set("Content-Type", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::SuccessfulNativeLogin`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::SuccessfulNativeLogin`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<UpdateLoginFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// This endpoint logs out an identity in a self-service manner.  If the `Accept` HTTP header is not set to `application/json`, the browser will be redirected (HTTP 303 See Other) to the `return_to` parameter of the initial request or fall back to `urls.default_return_to`.  If the `Accept` HTTP header is set to `application/json`, a 204 No Content response will be sent on successful logout instead.  This endpoint is NOT INTENDED for API clients and only works with browsers (Chrome, Firefox, ...). For API clients you can call the `/self-service/logout/api` URL directly with the Ory Session Token.  More information can be found at [Ory Kratos User Logout Documentation](https://www.ory.sh/docs/next/kratos/self-service/flows/user-logout).
pub async fn update_logout_flow(
    configuration: &configuration::Configuration,
    token: Option<&str>,
    return_to: Option<&str>,
    cookie: Option<&str>,
) -> Result<(), Error<UpdateLogoutFlowError>> {
    let client = RequestInit::new();
    client.set_method("GET");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/logout".len()
        + (configuration.base_path.len() * 2)
        + "return_to".len()
        + "token".len()
        + 4;

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/logout");

    let mut is_first_query: bool = true;

    if let Some(ref str) = return_to {
        uri_str.add_query(&mut is_first_query, "return_to=", &str);
    }
    if let Some(ref str) = token {
        uri_str.add_query(&mut is_first_query, "token=", &str);
    }

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        Ok(())
    } else {
        let entity: Option<UpdateLogoutFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Use this endpoint to update a recovery flow. This endpoint behaves differently for API and browser flows and has several states:  `choose_method` expects `flow` (in the URL query) and `email` (in the body) to be sent and works with API- and Browser-initiated flows. For API clients and Browser clients with HTTP Header `Accept: application/json` it either returns a HTTP 200 OK when the form is valid and HTTP 400 OK when the form is invalid. and a HTTP 303 See Other redirect with a fresh recovery flow if the flow was otherwise invalid (e.g. expired). For Browser clients without HTTP Header `Accept` or with `Accept: text/_*` it returns a HTTP 303 See Other redirect to the Recovery UI URL with the Recovery Flow ID appended. `sent_email` is the success state after `choose_method` for the `link` method and allows the user to request another recovery email. It works for both API and Browser-initiated flows and returns the same responses as the flow in `choose_method` state. `passed_challenge` expects a `token` to be sent in the URL query and given the nature of the flow (\"sending a recovery link\") does not have any API capabilities. The server responds with a HTTP 303 See Other redirect either to the Settings UI URL (if the link was valid) and instructs the user to update their password, or a redirect to the Recover UI URL with a new Recovery Flow ID which contains an error message that the recovery link was invalid.  More information can be found at [Ory Kratos Account Recovery Documentation](../self-service/flows/account-recovery).
pub async fn update_recovery_flow(
    configuration: &configuration::Configuration,
    flow: &str,
    update_recovery_flow_body: models::UpdateRecoveryFlowBody,
    token: Option<&str>,
    cookie: Option<&str>,
) -> Result<models::RecoveryFlow, Error<UpdateRecoveryFlowError>> {
    let client = RequestInit::new();
    client.set_method("POST");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/recovery".len()
        + (configuration.base_path.len() * 2)
        + "?flow=".len()
        + flow.len()
        + "&token=".len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/recovery");

    uri_str.push_str("?flow=");
    uri_str.push_str(&flow);

    if let Some(ref str) = token {
        uri_str.push_str("&token=");
        uri_str.push_str(&str);
    }

    client.set_body(&JsValue::from_serde(&update_recovery_flow_body)?);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;
    req_builder
        .headers()
        .set("Content-Type", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::RecoveryFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::RecoveryFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<UpdateRecoveryFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Use this endpoint to complete a registration flow by sending an identity's traits and password. This endpoint behaves differently for API and browser flows.  API flows expect `application/json` to be sent in the body and respond with HTTP 200 and a application/json body with the created identity success - if the session hook is configured the `session` and `session_token` will also be included; HTTP 410 if the original flow expired with the appropriate error messages set and optionally a `use_flow_id` parameter in the body; HTTP 400 on form validation errors.  Browser flows expect a Content-Type of `application/x-www-form-urlencoded` or `application/json` to be sent in the body and respond with a HTTP 303 redirect to the post/after registration URL or the `return_to` value if it was set and if the registration succeeded; a HTTP 303 redirect to the registration UI URL with the flow ID containing the validation errors otherwise.  Browser flows with an accept header of `application/json` will not redirect but instead respond with HTTP 200 and a application/json body with the signed in identity and a `Set-Cookie` header on success; HTTP 303 redirect to a fresh login flow if the original flow expired with the appropriate error messages set; HTTP 400 on form validation errors.  If this endpoint is called with `Accept: application/json` in the header, the response contains the flow without a redirect. In the case of an error, the `error.id` of the JSON response body can be one of:  `session_already_available`: The user is already signed in. `security_csrf_violation`: Unable to fetch the flow because a CSRF violation occurred. `security_identity_mismatch`: The requested `?return_to` address is not allowed to be used. Adjust this in the configuration! `browser_location_change_required`: Usually sent when an AJAX request indicates that the browser needs to open a specific URL. Most likely used in Social Sign In flows.  More information can be found at [Ory Kratos User Login](https://www.ory.sh/docs/kratos/self-service/flows/user-login) and [User Registration Documentation](https://www.ory.sh/docs/kratos/self-service/flows/user-registration).
pub async fn update_registration_flow(
    configuration: &configuration::Configuration,
    flow: &str,
    update_registration_flow_body: models::UpdateRegistrationFlowBody,
    cookie: Option<&str>,
) -> Result<models::SuccessfulNativeRegistration, Error<UpdateRegistrationFlowError>> {
    let client = RequestInit::new();
    client.set_method("POST");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/registration".len()
        + configuration.base_path.len()
        + "?flow=".len()
        + flow.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/registration");

    uri_str.push_str("?flow=");
    uri_str.push_str(&flow);

    client.set_body(&JsValue::from_serde(&update_registration_flow_body)?);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;
    req_builder
        .headers()
        .set("Content-Type", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::SuccessfulNativeRegistration`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::SuccessfulNativeRegistration`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<UpdateRegistrationFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Use this endpoint to complete a settings flow by sending an identity's updated password. This endpoint behaves differently for API and browser flows.  API-initiated flows expect `application/json` to be sent in the body and respond with HTTP 200 and an application/json body with the session token on success; HTTP 303 redirect to a fresh settings flow if the original flow expired with the appropriate error messages set; HTTP 400 on form validation errors. HTTP 401 when the endpoint is called without a valid session token. HTTP 403 when `selfservice.flows.settings.privileged_session_max_age` was reached or the session's AAL is too low. Implies that the user needs to re-authenticate.  Browser flows without HTTP Header `Accept` or with `Accept: text/_*` respond with a HTTP 303 redirect to the post/after settings URL or the `return_to` value if it was set and if the flow succeeded; a HTTP 303 redirect to the Settings UI URL with the flow ID containing the validation errors otherwise. a HTTP 303 redirect to the login endpoint when `selfservice.flows.settings.privileged_session_max_age` was reached or the session's AAL is too low.  Browser flows with HTTP Header `Accept: application/json` respond with HTTP 200 and a application/json body with the signed in identity and a `Set-Cookie` header on success; HTTP 303 redirect to a fresh login flow if the original flow expired with the appropriate error messages set; HTTP 401 when the endpoint is called without a valid session cookie. HTTP 403 when the page is accessed without a session cookie or the session's AAL is too low. HTTP 400 on form validation errors.  Depending on your configuration this endpoint might return a 403 error if the session has a lower Authenticator Assurance Level (AAL) than is possible for the identity. This can happen if the identity has password + webauthn credentials (which would result in AAL2) but the session has only AAL1. If this error occurs, ask the user to sign in with the second factor (happens automatically for server-side browser flows) or change the configuration.  If this endpoint is called with a `Accept: application/json` HTTP header, the response contains the flow without a redirect. In the case of an error, the `error.id` of the JSON response body can be one of:  `session_refresh_required`: The identity requested to change something that needs a privileged session. Redirect the identity to the login init endpoint with query parameters `?refresh=true&return_to=<the-current-browser-url>`, or initiate a refresh login flow otherwise. `security_csrf_violation`: Unable to fetch the flow because a CSRF violation occurred. `session_inactive`: No Ory Session was found - sign in a user first. `security_identity_mismatch`: The flow was interrupted with `session_refresh_required` but apparently some other identity logged in instead. `security_identity_mismatch`: The requested `?return_to` address is not allowed to be used. Adjust this in the configuration! `browser_location_change_required`: Usually sent when an AJAX request indicates that the browser needs to open a specific URL. Most likely used in Social Sign In flows.  More information can be found at [Ory Kratos User Settings & Profile Management Documentation](../self-service/flows/user-settings).
pub async fn update_settings_flow(
    configuration: &configuration::Configuration,
    flow: &str,
    update_settings_flow_body: models::UpdateSettingsFlowBody,
    x_session_token: Option<&str>,
    cookie: Option<&str>,
) -> Result<models::SettingsFlow, Error<UpdateSettingsFlowError>> {
    let client = RequestInit::new();
    client.set_method("POST");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/settings".len()
        + configuration.base_path.len()
        + "?flow=".len()
        + flow.len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/settings");

    uri_str.push_str("?flow=");
    uri_str.push_str(&flow);

    client.set_body(&JsValue::from_serde(&update_settings_flow_body)?);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }
    if let Some(x_session_token) = x_session_token {
        req_builder
            .headers()
            .set("X-Session-Token", x_session_token)?;
    }

    req_builder.headers().set("Accept", "application/json")?;
    req_builder
        .headers()
        .set("Content-Type", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::SettingsFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::SettingsFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<UpdateSettingsFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}

/// Use this endpoint to complete a verification flow. This endpoint behaves differently for API and browser flows and has several states:  `choose_method` expects `flow` (in the URL query) and `email` (in the body) to be sent and works with API- and Browser-initiated flows. For API clients and Browser clients with HTTP Header `Accept: application/json` it either returns a HTTP 200 OK when the form is valid and HTTP 400 OK when the form is invalid and a HTTP 303 See Other redirect with a fresh verification flow if the flow was otherwise invalid (e.g. expired). For Browser clients without HTTP Header `Accept` or with `Accept: text/_*` it returns a HTTP 303 See Other redirect to the Verification UI URL with the Verification Flow ID appended. `sent_email` is the success state after `choose_method` when using the `link` method and allows the user to request another verification email. It works for both API and Browser-initiated flows and returns the same responses as the flow in `choose_method` state. `passed_challenge` expects a `token` to be sent in the URL query and given the nature of the flow (\"sending a verification link\") does not have any API capabilities. The server responds with a HTTP 303 See Other redirect either to the Settings UI URL (if the link was valid) and instructs the user to update their password, or a redirect to the Verification UI URL with a new Verification Flow ID which contains an error message that the verification link was invalid.  More information can be found at [Ory Kratos Email and Phone Verification Documentation](https://www.ory.sh/docs/kratos/self-service/flows/verify-email-account-activation).
pub async fn update_verification_flow(
    configuration: &configuration::Configuration,
    flow: &str,
    update_verification_flow_body: models::UpdateVerificationFlowBody,
    token: Option<&str>,
    cookie: Option<&str>,
) -> Result<models::VerificationFlow, Error<UpdateVerificationFlowError>> {
    let client = RequestInit::new();
    client.set_method("POST");
    client.set_mode(RequestMode::Cors);
    client.set_credentials(RequestCredentials::Include);

    let possible_uri_len = "/self-service/verification".len()
        + (configuration.base_path.len() * 2)
        + "?flow=".len()
        + flow.len()
        + "&token=".len();

    let mut uri_str = String::with_capacity(possible_uri_len);

    uri_str.push_str(&configuration.base_path);
    uri_str.push_str("/self-service/verification");

    uri_str.push_str("?flow=");
    uri_str.push_str(&flow);

    if let Some(ref str) = token {
        uri_str.push_str("&token=");
        uri_str.push_str(&str);
    }

    client.set_body(&JsValue::from_serde(&update_verification_flow_body)?);

    let req_builder = Request::new_with_str_and_init(&uri_str, &client)?;

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder.headers().set("USER_AGENT", user_agent)?;
    }
    if let Some(cookie) = cookie {
        req_builder.headers().set("Cookie", cookie)?;
    }

    req_builder.headers().set("Accept", "application/json")?;
    req_builder
        .headers()
        .set("Content-Type", "application/json")?;

    let req = JsFuture::from(web_sys::window().unwrap().fetch_with_request(&req_builder)).await?;

    assert!(req.is_instance_of::<Response>());
    let resp: Response = req.dyn_into().unwrap();

    let status = resp.status();
    let content_type = super::ContentType::from(&resp);
    let content = JsFuture::from(resp.json()?).await?;

    if !(400..600).contains(&status) {
        match content_type {
            ContentType::Json => content.into_serde().map_err(Error::from),
            ContentType::Text => {
                return Err(Error::from(serde_json::Error::custom(
                    "Received `text/plain` content type response that cannot be converted to `models::VerificationFlow`",
                )));
            }
            ContentType::Unsupported(unknown_type) => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received `{unknown_type}` content type response that cannot be converted to `models::VerificationFlow`"
                ))));
            }
            ContentType::Missing => {
                return Err(Error::from(serde_json::Error::custom(format!(
                    "Received response that is missing `content-type` header"
                ))));
            }
        }
    } else {
        let entity: Option<UpdateVerificationFlowError> = content.into_serde().ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content: if content.is_undefined() {
                String::from("null")
            } else {
                web_sys::js_sys::JSON::stringify(&content)
                    .map(String::from)
                    .unwrap_throw()
            },
            entity,
        }))
    }
}
