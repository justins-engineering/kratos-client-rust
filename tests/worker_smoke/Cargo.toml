[package]
name = "ory-kratos-client-wasm-worker-smoke"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = "0.8"
ory-kratos-client-wasm = { path = "../..", default-features = false, features = [
  "worker",
] }
